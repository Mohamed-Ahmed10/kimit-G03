export default function CourseView({course_name , course_desc}) {
    return (
        <div>
            <h2>{course_name}</h2>
            <p>{course_desc}</p>
            <hr />
        </div>
    )
}
